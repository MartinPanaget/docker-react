language: generic
sudo: required
services: 
  - docker

before_install:
  - docker build -t martinpanaget/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true martinpanaget/docker-react npm run test
